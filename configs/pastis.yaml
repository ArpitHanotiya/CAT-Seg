dataset:
  name: 'pastis'
  root: '/kaggle/input/pastis-dataset-ddp/PASTIS'
  temporal_mode: 'mean'

model:
  clip_model: 'ViT-B/16'
  input_channels: 10
  num_classes: 19

training:
  batch_size: 8
  lr: 0.0001
  epochs: 50